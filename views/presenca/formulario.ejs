<!-- <% const erros = validationErrors(locals.errors); %>
<% const alunos = locals.alunos || [] %>
<% const turma = locals.turma || {} %>
<% const presenca = locals.presenca || {} %>

<div class="row page-titles mx-0">
    <div class="col-sm-6 p-md-0">
        <div class="welcome-text">
            <h4><%= locals.titulo %></h4>
            <p class="mb-0">&raquo; Lista de presença</p>
        </div>
    </div>

    <div class="col-sm-6 p-md-0 justify-content-sm-end mt-2 mt-sm-0 d-flex">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Inicial</a></li>
            <li class="breadcrumb-item"><a href="/turmas">Turmas</a></li>
            <li class="breadcrumb-item active">Presença</li>
        </ol>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">

        <% if (erros.all && erros.all.length) { %>
            <div class="alert alert-danger">
                <h5>Erro ao salvar presença</h5>
                <ul>
                    <% erros.all.forEach((mensagem) => { %>
                        <li><%= mensagem %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <div class="card">
            <div class="card-body">

                <form action="/presenca/salvar" method="POST">

                    <input type="hidden" name="turma_id" value="<%= turma.id %>">

                    <div class="mb-3">
                        <h5>Turma: <%= turma.nome %></h5>
                    </div>

                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Aluno</th>
                                <th>Presença</th>
                            </tr>
                        </thead>

                        <tbody>
                            <% alunos.forEach((aluno, index) => { %>
                                <tr>
                                    <td>
                                        <%= aluno.nome %>
                                        <input type="hidden" name="alunos[<%= index %>][id]" value="<%= aluno.id %>">
                                    </td>

                                    <td>
                                        <select name="alunos[<%= index %>][presenca]" class="form-control">
                                            <option value="presente">Presente</option>
                                            <option value="falta">Falta</option>
                                        </select>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>

                    <div class="form-group mt-3">
                        <button type="submit" class="btn btn-primary">
                            Salvar Presença
                        </button>

                        <a href="/turmas" class="btn btn-warning">
                            Cancelar
                        </a>
                    </div>

                </form>

            </div>
        </div>

    </div>
</div> -->